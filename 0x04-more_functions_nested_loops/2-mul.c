#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 *
 * Return: product as int
 */
int mul(int a, int b)
{
	if (int a >= '0' && int a <= '9')
	{
		if (int b >= '0' && int a <= '9')
		{
			return (a * b);
		}
	}
}
