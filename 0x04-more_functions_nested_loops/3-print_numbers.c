#include "main.h"

/*
 * print_numbers - prints numbers from 0 to 9
 * followed by a new line
 *
 * Return: Always 0.
 *
 */
void print_numbers(void)
{
	int a;

	while (a >= '0' && a <= '9' ; i++)
	{
		_putchar(i);
		_putchar(\n);
	}
	return (0);
}
